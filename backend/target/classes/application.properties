# Define o nome da aplica��o no contexto Spring Boot.
spring.application.name=backend

# Especifica o dialeto SQL utilizado pelo Hibernate. 
# Aqui est� configurado para MySQL, permitindo a gera��o correta das queries.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Define a classe do driver JDBC para conex�o com o banco de dados MySQL.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# URL de conex�o para o banco de dados MySQL, incluindo o endere�o do servidor, a porta, 
# e o nome do banco de dados. Aqui, conecta-se ao servi�o Aiven.
spring.datasource.url=jdbc:mysql://mysql-22d05629-matheusmrc7-d9cf.i.aivencloud.com:24875/movie 

# Nome de usu�rio utilizado para se conectar ao banco de dados.
spring.datasource.username=avnadmin

# Senha utilizada para se conectar ao banco de dados.
spring.datasource.password=AVNS_Sm_3OkpNbMs_j_R51oh

# Habilita a gera��o autom�tica do esquema do banco de dados (DDL - Data Definition Language).
spring.jpa.generate-ddl=true

# Define a estrat�gia para o Hibernate atualizar o esquema do banco de dados. 
# "update" significa que o Hibernate atualiza o esquema sem apagar dados existentes.
spring.jpa.hibernate.ddl-auto=update

# Habilita a exibi��o das queries SQL geradas pelo Hibernate no console para facilitar o debug.
spring.jpa.show-sql=true

# Define a porta em que a aplica��o Spring Boot ir� rodar.
server.port=8080 